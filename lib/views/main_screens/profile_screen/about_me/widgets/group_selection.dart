import 'package:flutter/material.dart';

class GroupSelection extends StatelessWidget {
  const GroupSelection({Key? key, this.title}) : super(key: key);
  final String? title;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
